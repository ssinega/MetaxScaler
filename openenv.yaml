name: cloud-cost-optimizer-env
description: AI environment for optimizing cloud infrastructure costs (FinOps)
version: "0.1.0"
python_version: "3.10"
entry_point:
  module: env
  class: SupportEnv
tasks:
  - easy
  - medium
  - hard
tasks_module: env.tasks
graders_module: env.graders
default_max_steps: 5
metadata:
  domain: cloud-infrastructure
  action_space: discrete
  observation_type: structured
  tags:
    - openenv
    - finops
    - cloud-optimization
    - aws-azure-gcp
